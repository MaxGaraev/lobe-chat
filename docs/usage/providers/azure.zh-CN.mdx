---
title: 在 GDZ.WORKS 中使用 Azure OpenAI API Key
description: 学习如何在 GDZ.WORKS 中配置和使用 Azure OpenAI 模型进行对话，包括获取 API 密钥和选择模型。
tags:
  - Azure OpenAI
  - API Key
  - Web UI
---

# 在 GDZ.WORKS 中使用 Azure OpenAI

本文档将指导你如何在 GDZ.WORKS 中使用 [Azure OpenAI](https://oai.azure.com/):

<Steps>
  ### 步骤一：获取 Azure OpenAI API 密钥

  - 如果尚未注册，则必须注册 [Azure OpenAI 帐户](https://oai.azure.com/)。

  - 注册完毕后，转到 `Deployments` 页面，然后使用您选择的模型创建新部署。

  - 转到 `Chat` 页面，然后单击 `View Code` 以获取您的终结点和密钥。

  ### 步骤二：在 GDZ.WORKS 中配置 Azure OpenAI

  - 访问 GDZ.WORKS 的`设置`界面

  - 在`语言模型`下找到`Azure OpenAI`的设置项

  - 填入获得的 API 密钥

  - 为你的 AI 助手选择一个 Azure OpenAI 的模型即可开始对话

  <Callout type={'warning'}>
    在使用过程中你可能需要向 API 服务提供商付费，请参考 Azure OpenAI 的相关费用政策。
  </Callout>
</Steps>

至此你已经可以在 GDZ.WORKS 中使用 Azure OpenAI 提供的模型进行对话了。
